---
phase: 01-loading-core-foundation
plan: 04
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/shared/loading/FullPageLoadingScreen.tsx
  - src/shared/loading/LoadingSpinner.tsx
  - components/ui/loader-15.tsx
  - src/shared/loading/loading-ui.ts
  - src/shared/loading/FullPageLoadingScreen.test.tsx
autonomous: true
requirements: [LOAD-01, LOAD-03, FLOW-01]
user_setup: []
must_haves:
  truths:
    - "Canonical full-page loader matches agreed minimal visual style and copy (`Lade...`) without divergent helper text."
    - "Spinner rendering uses the requested loader-15 implementation path with safe fallback behavior."
    - "Background treatment supports lighter appearance for short loads and optional transparent/white switch scenarios."
  artifacts:
    - path: src/shared/loading/FullPageLoadingScreen.tsx
      provides: canonical loading overlay visual contract aligned with UAT feedback
      min_lines: 80
    - path: src/shared/loading/LoadingSpinner.tsx
      provides: spinner adapter resolving to agreed loader implementation
      min_lines: 30
    - path: src/shared/loading/FullPageLoadingScreen.test.tsx
      provides: regression coverage for copy/background/spinner contract
      min_lines: 70
  key_links:
    - from: src/shared/loading/FullPageLoadingScreen.tsx
      to: src/shared/loading/loading-ui.ts
      via: background/message variants derive from shared UI policy constants
    - from: src/shared/loading/LoadingSpinner.tsx
      to: components/ui/loader-15.tsx
      via: canonical loader uses requested loader component path first
---

<objective>
Align canonical loading visuals and spinner implementation with UAT agreements.

Purpose: Remove visual drift so users always see the expected modern/minimal loading treatment.
Output: Updated full-page loading UI + spinner adapter + regression tests enforcing the agreed presentation.
</objective>

<execution_context>
@/Users/tillschafer/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tillschafer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-loading-core-foundation/01-UAT.md
@src/shared/loading/FullPageLoadingScreen.tsx
@src/shared/loading/LoadingSpinner.tsx
@components/ui/loader-15.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine canonical loading surface to agreed minimal style</name>
  <files>
    src/shared/loading/loading-ui.ts
    src/shared/loading/FullPageLoadingScreen.tsx
  </files>
  <action>
    Update full-page loader markup/styles so it follows the confirmed UAT direction:
    - primary text remains `Lade...` fallback (no always-on secondary helper sentence)
    - modern/elegant/minimal layout with lighter background treatment
    - preserve full-screen blocking semantics and short-load lighter variant behavior
    - add deterministic style variant handling for transparent vs full-white transitions when context requires it
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Loading screen appearance and copy match the agreed style contract across hydration and route transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize spinner source to requested loader-15 implementation</name>
  <files>
    components/ui/loader-15.tsx
    src/shared/loading/LoadingSpinner.tsx
  </files>
  <action>
    Ensure the spinner path reflects the requested loader:
    - replace local placeholder loader implementation with the agreed loader-15 source-compatible implementation
    - keep fallback behavior if loader cannot render (safety for runtime resilience)
    - avoid wrapper styles that alter the loader visual unexpectedly
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Rendered spinner matches the expected loader family and no longer appears as a divergent custom fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Expand canonical loading UI regression tests</name>
  <files>src/shared/loading/FullPageLoadingScreen.test.tsx</files>
  <action>
    Add/adjust tests to lock in UAT expectations:
    - canonical fallback copy rendering without legacy helper text dependency
    - lighter/transparent/white background variant class behavior
    - spinner path remains canonical (`loader-15`) and not legacy icon fallback in normal setup
  </action>
  <verify>
    <automated>npm run test -- src/shared/loading/FullPageLoadingScreen.test.tsx</automated>
  </verify>
  <done>UI test suite protects against recurrence of the loader style/spinner drift reported in UAT.</done>
</task>

</tasks>

<verification>
Canonical loading screen and spinner now align with agreed visuals and are covered by regression tests.
</verification>

<success_criteria>
- [ ] Canonical loading copy and styling match UAT decisions.
- [ ] Spinner source uses requested loader-15 implementation path.
- [ ] Background variants support lighter and contextual transparent/white treatment.
- [ ] Updated loading UI tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-loading-core-foundation/01-04-SUMMARY.md`
</output>
