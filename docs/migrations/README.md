# docs/migrations (Archive)

This directory contains historical SQL files from the legacy migration workflow.

## Status
- Archive/reference only.
- Do not add new operational migrations here.

## Canonical Path (current)
Use `supabase/migrations` for all new migrations and CLI-driven migration operations.
